# DisplayPicture - 显示图片并轮询旋转

## 概述
本示例显示内置图片（`imga_data`），并每 2 秒在 0/90/180/270 度四种旋转之间轮换。

## 硬件要求
- TUYA_T5AI 开发板（已接入并支持的显示屏）

## 使用方法
1. 上传示例程序。
2. 打开串口监视器（115200）。
3. 图片会显示在屏幕上，并每 2 秒旋转一次。

## 说明
- 图片数据由示例源文件提供（例如 `ImageData.c`）。
- 旋转通过 `display.setRotation(...)` 生效。
